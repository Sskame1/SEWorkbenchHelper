<Window x:Class="SEWorkbenchHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SEWorkbenchHelper"
        mc:Ignorable="d"
        Title="SEWorkbenchHelper" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- List File -->
        <ListBox x:Name="FilesListBox" Grid.Column="0" Margin="5"
                 SelectionChanged="FilesListBox_SelectionChanged"/>

        <!-- The area for the file contents -->
        <TextBox x:Name="FileContentTextBox" Grid.Column="1" Margin="5" 
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto" />
    </Grid>
</Window>
